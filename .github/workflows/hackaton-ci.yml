name: Hackaton 2026 CI

on:
  push:
    branches: [hackaton_2026, develop, main]
  pull_request:
    branches: [hackaton_2026, develop, main]
  workflow_dispatch:

jobs:
  hackaton-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Check Python syntax
        run: |
          python -m compileall app app.py tests

      - name: Validate project structure
        run: |
          test -f app.py
          test -d app
          test -d tests
          test -f requirements.txt
          test -f app/services/code_runner_service.py
          test -f app/routes/lecturer.py
          echo "Project structure validation passed"

      - name: Validate hackaton files
        run: |
          test -f .github/workflows/hackaton-ci.yml
          echo "Hackaton CI workflow exists"

      - name: Run Hackaton related tests if available
        run: |
          TEST_FILES=""

          for file in \
            tests/test_design_system.py \
            tests/test_code_runner.py \
            tests/test_seed.py \
            tests/test_basic.py
          do
            if [ -f "$file" ]; then
              TEST_FILES="$TEST_FILES $file"
            fi
          done

          if [ -z "$TEST_FILES" ]; then
            echo "No stable hackaton test files found. Running syntax and structure validation only."
          else
            echo "Running stable hackaton test files: $TEST_FILES"
            pytest $TEST_FILES -v
          fi
